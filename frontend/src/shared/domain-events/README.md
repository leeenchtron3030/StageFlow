# Shared Domain Events

## Purpose

Contains the frontend base domain event shape.

## Current Scope

ED-0004 defines only the generic `DomainEvent` type. StageFlow-specific events belong to future bounded-context directives.
